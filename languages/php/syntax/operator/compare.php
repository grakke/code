<?php


## 比较运算符
var_dump(10 <=> 12);
var_dump(13 <=> 12);
var_dump(12 <=> 12);

# == vs ===: 只比较变量值 除了比较变量值，还会比较变量类型
$c = 32;
$d = 32.0;
var_dump($c == $d);
var_dump($c === $d);

echo $a < $b; # ($a <=> $b) === -1
echo $a <= $b;   # ($a <=> $b) === -1 || ($a <=> $b) === 0
echo $b == $a; #  ($a <=> $b) === 0
echo $a != $b; #($a <=> $b) !== 0
echo $a >= $b;# ($a <=> $b) === 1 || ($a <=> $b) === 0
echo $a > $b;#($a <=> $b) === 1
